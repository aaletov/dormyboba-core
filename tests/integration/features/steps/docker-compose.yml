version: "3.1"

services:
  dormyboba_core:
    image: "dormyboba-core:latest"
    ports:
      - "50051:50051"
      - "8000:8000"
  postgresql:
    image: "postgres:15.5-alpine3.18"
    ports:
      - "5432:5432"
    environment:
     POSTGRES_PASSWORD: "123456"
